j'ai desasemle le main avec gdb et j'ai vu qu'il utilisait uniquement getuid. Le code nous demande d'avoir l'uid 4242 pour avoir le token. J'en ai en conclu 2 solutions : 

modifier la valeur de retour de getuid dans gdb et de passer le if.
utiliser LD_PRELOAD pour partager une librairie qui contient un fake getuid qui retournerait 4242.

J'ai pris la deuxieme option car j'ai pas le level en assembleur pour faire la premiere option.

je te met les commandes que j'ai faites : 

   gcc -o getuid.so -fPIC -shared getuid.c
   export LD_PRELOAD=/home/user/level13/getuid.so

J'ai lance ./level13 et rien ne changeait. J'ai voulu lance gdb pour voir et ca a marcher.

Je comprend pas pourquoi ca marche avec gdb et pas quand je lance normalement. J'ai pas trouve d'explication pour le moment.
